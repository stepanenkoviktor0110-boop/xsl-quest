# Настройка Git и загрузка на GitHub

## Шаг 1: Настройте Git (выполните один раз)

```bash
git config --global user.name "Ваше Имя"
git config --global user.email "ваш-email@example.com"
```

Например:
```bash
git config --global user.name "Ivan Petrov"
git config --global user.email "ivan.petrov@gmail.com"
```

## Шаг 2: Создайте коммит

```bash
cd "D:\МОИ ПРОЕКТЫ\XSL Quest\XSL Quest"

git commit -m "Initial commit: XSL Quest - генератор тестов из Excel"
```

## Шаг 3: Создайте репозиторий на GitHub

1. Перейдите на https://github.com
2. Войдите в свой аккаунт (или зарегистрируйтесь)
3. Нажмите кнопку "+" в правом верхнем углу → "New repository"
4. Заполните:
   - **Repository name:** xsl-quest
   - **Description:** Генератор тестов из Excel файлов
   - **Public** или **Private** (на ваш выбор)
   - ❌ НЕ создавайте README, .gitignore, license (они уже есть)
5. Нажмите "Create repository"

## Шаг 4: Подключите локальный репозиторий к GitHub

После создания репозитория GitHub покажет инструкции. Выполните:

```bash
cd "D:\МОИ ПРОЕКТЫ\XSL Quest\XSL Quest"

git branch -M main
git remote add origin https://github.com/ваш-username/xsl-quest.git
git push -u origin main
```

Замените `ваш-username` на ваше имя пользователя GitHub!

## Шаг 5: Введите credentials (если потребуется)

При первом push Git может попросить авторизацию:

**Вариант 1: Personal Access Token (рекомендуется)**

1. GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)
2. Generate new token (classic)
3. Выберите срок действия и права (нужно `repo`)
4. Скопируйте токен
5. При запросе пароля вставьте токен вместо пароля

**Вариант 2: GitHub CLI**

```bash
# Установите GitHub CLI: https://cli.github.com/
gh auth login
```

## Готово! 🎉

Теперь ваш проект на GitHub! Можно использовать для развертывания на Render.com или Railway.app

## Полезные команды Git

```bash
# Проверить статус
git status

# Добавить изменения
git add .

# Создать коммит
git commit -m "Описание изменений"

# Отправить на GitHub
git push

# Посмотреть историю
git log --oneline
```
