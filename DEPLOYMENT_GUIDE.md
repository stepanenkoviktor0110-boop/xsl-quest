# Руководство по развертыванию XSL Quest

## Текущий статус
✅ Сервер запущен на http://localhost:5000
✅ Доступен в локальной сети

## Вариант 1: Ngrok (Быстрое тестирование)

### Установка ngrok

**Способ 1: Через npm**
```bash
npm install -g ngrok
```

**Способ 2: Скачать с сайта**
1. Перейти на https://ngrok.com/download
2. Скачать для Windows
3. Распаковать в удобную папку

### Запуск

1. Убедитесь, что ваш сервер запущен:
```bash
npm start
```

2. В новом терминале запустите ngrok:
```bash
ngrok http 5000
```

3. Вы получите публичную ссылку:
```
Forwarding: https://abc123.ngrok-free.app -> http://localhost:5000
```

**Эту ссылку можно отправить кому угодно в интернете!**

### Преимущества ngrok
✅ Работает мгновенно
✅ HTTPS из коробки
✅ Не нужно настраивать роутер/firewall
✅ Отлично для тестирования

### Недостатки ngrok
❌ Ссылка меняется при каждом перезапуске (в бесплатной версии)
❌ Ограничения на трафик
❌ Компьютер должен быть включен

---

## Вариант 2: LocalTunnel (Альтернатива ngrok)

### Установка
```bash
npm install -g localtunnel
```

### Запуск
```bash
lt --port 5000
```

Получите ссылку типа: https://random-subdomain.loca.lt

---

## Вариант 3: Render.com (Бесплатный хостинг)

### Подготовка

1. **Создайте Git репозиторий:**
```bash
git init
git add .
git commit -m "Initial commit"
```

2. **Загрузите на GitHub:**
- Создайте репозиторий на GitHub
- Выполните:
```bash
git remote add origin https://github.com/ваш-username/xsl-quest.git
git push -u origin main
```

### Развертывание на Render

1. Зарегистрируйтесь на https://render.com
2. Нажмите "New +" → "Web Service"
3. Подключите GitHub репозиторий
4. Настройки:
   - **Name:** xsl-quest
   - **Environment:** Node
   - **Build Command:** `cd backend && npm install`
   - **Start Command:** `node backend/server.js`
   - **Plan:** Free

5. Нажмите "Create Web Service"

Через 5-10 минут приложение будет доступно по адресу:
`https://xsl-quest.onrender.com`

### Преимущества Render
✅ Бесплатный план
✅ Автоматические деплои при push в GitHub
✅ HTTPS из коробки
✅ Работает 24/7

### Недостатки Render
❌ "Засыпает" после 15 минут неактивности (на бесплатном плане)
❌ Первый запрос после сна медленный (~30 сек)

---

## Вариант 4: Railway.app (Простой хостинг)

1. Зарегистрируйтесь на https://railway.app
2. Подключите GitHub репозиторий
3. Railway автоматически определит Node.js проект
4. Добавьте переменную окружения (если нужно):
   - `PORT=5000`

Приложение будет доступно на `https://ваш-проект.railway.app`

### Преимущества Railway
✅ $5 бесплатного кредита каждый месяц
✅ Не "засыпает"
✅ Очень простая настройка

---

## Вариант 5: Docker + VPS (DigitalOcean, Hetzner)

### 1. Постройте Docker образ локально (для теста)

```bash
docker build -t xsl-quest .
docker run -p 5000:5000 xsl-quest
```

### 2. Развертывание на VPS

**Подключитесь к серверу:**
```bash
ssh root@ваш-ip
```

**Установите Docker:**
```bash
curl -fsSL https://get.docker.com -o get-docker.sh
sh get-docker.sh
```

**Клонируйте проект:**
```bash
git clone https://github.com/ваш-username/xsl-quest.git
cd xsl-quest
```

**Запустите:**
```bash
docker build -t xsl-quest .
docker run -d -p 80:5000 --name xsl-quest xsl-quest
```

**Настройте Nginx (опционально, для HTTPS):**
```bash
apt install nginx certbot python3-certbot-nginx
```

Создайте `/etc/nginx/sites-available/xsl-quest`:
```nginx
server {
    listen 80;
    server_name ваш-домен.com;

    location / {
        proxy_pass http://localhost:5000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_cache_bypass $http_upgrade;
    }
}
```

```bash
ln -s /etc/nginx/sites-available/xsl-quest /etc/nginx/sites-enabled/
nginx -t
systemctl restart nginx
certbot --nginx -d ваш-домен.com
```

---

## Рекомендации по выбору

### Для быстрого тестирования (5 минут):
👉 **Ngrok** или **LocalTunnel**

### Для демонстрации проекта друзьям/коллегам:
👉 **Render.com** или **Railway.app**

### Для production использования:
👉 **VPS + Docker + Nginx**

---

## Проверка работоспособности

После развертывания проверьте:

1. **Главная страница:** `https://ваш-домен/`
2. **Загрузка файла:** Загрузите Excel файл
3. **Тест по ссылке:** Скопируйте и откройте ссылку на тест
4. **Прохождение теста:** Выберите ответы и отправьте

---

## Поддержка

Если возникли проблемы:
- Проверьте логи: `docker logs xsl-quest` (если используете Docker)
- Проверьте порты: `netstat -ano | findstr :5000`
- Проверьте firewall: Windows Defender Firewall → Входящие правила
